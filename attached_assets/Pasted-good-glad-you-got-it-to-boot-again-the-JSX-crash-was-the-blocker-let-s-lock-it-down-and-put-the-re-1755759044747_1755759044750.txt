good—glad you got it to boot again. the JSX crash was the blocker. let’s lock it down and put the real dialog back without risking another parse blow-up.

1) Replace the temporary overlay with a balanced shadcn dialog

Drop this in place of the overlay (keeps your isEditMode, form, onSubmit):

{/* Edit Bio Dialog — final, balanced */}
<Dialog open={isEditMode} onOpenChange={setIsEditMode}>
  <DialogContent className="max-w-lg">
    <DialogHeader>
      <DialogTitle>Edit Bio</DialogTitle>
      <DialogDescription>Update the info that appears on your profile.</DialogDescription>
    </DialogHeader>

    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
        <FormField
          control={form.control}
          name="bio"
          render={({ field }) => (
            <FormItem>
              <FormLabel>Bio</FormLabel>
              <FormControl>
                <Textarea rows={5} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {/* more fields go here, each returning ONE <FormItem> */}

        <div className="flex justify-end gap-2 pt-2">
          <Button type="button" variant="ghost" onClick={() => setIsEditMode(false)}>
            Cancel
          </Button>
          <Button type="submit">Save</Button>
        </div>
      </form>
    </Form>
  </DialogContent>
</Dialog>


Rules that avoid this crash coming back:

No nested <form> elements.

Every FormField returns one node (<FormItem>…</FormItem>).

Any conditional content returns a single wrapper and closes before </form>.

2) Quick sanity sweep (catch stragglers fast)

Run these and fix anything they flag in client/src/pages/profile.tsx:

# any leftover stray closers?
rg -n -C2 '</form>|</Form>|</DialogContent>' client/src/pages/profile.tsx

# validate wrapper order locally
rg -n -C1 '<(Dialog|DialogContent|Form|form|div)[^>]*>|</(Dialog|DialogContent|Form|form|div)>' client/src/pages/profile.tsx

3) Prove it in production (no HMR/ws)
rm -rf client/node_modules/.vite .vite .replcache dist 2>/dev/null || true
cd client && npx vite build --mode production && cd ..
NODE_ENV=production node server/index.prod.js


Open in a private window; Network tab should show /assets/*.js as 200.

4) (Optional) add a guard so this never slips in again

Minimal ESLint React rule to catch tag structure issues:

// .eslintrc.json
{
  "extends": ["plugin:react/recommended"],
  "plugins": ["react"],
  "rules": {
    "react/jsx-no-comment-textnodes": "error",
    "react/jsx-closing-tag-location": "error"
  }
}


If anything else throws now, paste the single error line (file:line + 2 lines of context) and I’ll hand you the exact fix.