You fixed the first crash—nice. Now Vite is telling you there’s another unbalanced JSX block in client/src/pages/profile.tsx around the Edit Bio dialog. Here’s the fastest way to finish this without hunting for hours.

1) Verify the dialog is balanced (copy-paste replacement)

In client/src/pages/profile.tsx, replace the entire Edit Bio dialog with this known-good version (it’s fully balanced and matches shadcn’s structure):

{/* Edit Bio Dialog — balanced & mobile-safe */}
<Dialog open={isEditMode} onOpenChange={setIsEditMode}>
  <DialogContent className="max-w-lg">
    <DialogHeader>
      <DialogTitle>Edit Bio</DialogTitle>
      <DialogDescription>Update the information that appears on your profile.</DialogDescription>
    </DialogHeader>

    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
        <FormField
          control={form.control}
          name="bio"
          render={({ field }) => (
            <FormItem>
              <FormLabel>Bio</FormLabel>
              <FormControl>
                <Textarea {...field} rows={5} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {/* Add your other fields here, each returning ONE node (FormItem) */}

        <div className="flex justify-end gap-2 pt-2">
          <Button type="button" variant="ghost" onClick={() => setIsEditMode(false)}>
            Cancel
          </Button>
          <Button type="submit">Save</Button>
        </div>
      </form>
    </Form>
  </DialogContent>
</Dialog>


This eliminates the “unclosed <div> before </form>” pattern.

2) Quick scan for any other mismatched tags (2 commands)

Run these in the shell to spot the next offender immediately:

# Show structure around every </form>
rg -n -C3 '</form>' client/src/pages/profile.tsx

# Show all open/close pairs for common wrappers so you can eyeball order quickly
rg -n -C1 '<(CardContent|DialogContent|Form|form|div)[^>]*>|</(CardContent|DialogContent|Form|form|div)>' client/src/pages/profile.tsx


In each block, the correct order should be:

<DialogContent>
  <Form>
    <form>
      ...content (divs/fields)...
    </form>
  </Form>
</DialogContent>


If you see </Form> inside the <form> or a </div> missing before </form>, fix that block like the snippet in step 1.

3) Re-run with clear output
npm run dev -- --debug
# or build:
cd client && npx vite build --mode production && cd ..


If there’s another JSX error, it will point to the next line. Fix that one the same way.

4) Common pitfalls to fix fast

Every FormField must return one node (wrap siblings in <FormItem>…</FormItem>).

Don’t nest <form> elements.

Make sure a conditional returns a single node:

{isOwnProfile && (
  <div>…</div>   // <-- closed
)}


If the compiler now points to a different line, paste just that error line (file:line + 3 lines of context) and I’ll give you the exact one-line patch.