Found it! The problem is touchAction: 'manipulation' on the hamburger button.
This is the SAME issue we fixed earlier in the regular Navbar - touch-action: manipulation blocks clicks in Capacitor WebView.
Fix for MobileTopNav Hamburger:
Replace the hamburger button (around line 60-80) with this:
tsx<button
  type="button"
  aria-expanded={showDropdown}
  aria-controls="mobile-menu"
  className="hamburger-btn w-12 h-12 flex items-center justify-center rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 active:bg-gray-200 dark:active:bg-gray-700 cursor-pointer relative"
  style={{ 
    WebkitTapHighlightColor: 'rgba(0,0,0,0.1)', 
    cursor: 'pointer',
    touchAction: 'auto', // Changed from 'manipulation'
    zIndex: 50002,
    position: 'relative',
    isolation: 'isolate'
  }}
  onClick={(e) => {
    e.stopPropagation();
    console.log('🍔 MobileTopNav Hamburger clicked');
    setShowDropdown((s) => !s);
  }}
>
  {showDropdown ? <X className="w-6 h-6 pointer-events-none" /> : <Menu className="w-6 h-6 pointer-events-none" />}
</button>
Also remove these lines that conflict:
tsxuserSelect: 'none',
WebkitUserSelect: 'none',
Simplified version (recommended):
tsx<button
  type="button"
  aria-expanded={showDropdown}
  aria-controls="mobile-menu"
  className="hamburger-btn w-12 h-12 flex items-center justify-center rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 active:bg-gray-200 dark:active:bg-gray-700"
  onClick={() => {
    console.log('🍔 Hamburger clicked');
    setShowDropdown((s) => !s);
  }}
>
  {showDropdown ? <X className="w-6 h-6 pointer-events-none" /> : <Menu className="w-6 h-6 pointer-events-none" />}
</button>
Key changes:

✅ touchAction: 'manipulation' → 'auto' (or remove entirely)
✅ Remove userSelect: 'none' (not needed)
✅ Simplify onClick - remove e.stopPropagation() (not needed here)


Why profile page worked but others didn't:
The profile page likely has different scroll behavior or CSS that somehow bypassed the touch-action: manipulation block. Other pages probably have default scroll containers that conflict with it.
Apply both fixes: