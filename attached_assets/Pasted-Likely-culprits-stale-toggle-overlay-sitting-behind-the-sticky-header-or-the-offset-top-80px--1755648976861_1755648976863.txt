Likely culprits: stale toggle, overlay sitting behind the sticky header, or the offset (top-[80px]) not matching your real header height. Here’s a drop-in fix that covers all three.

1) Make the toggle bulletproof (use functional updater; no preventDefault)
<Button
  variant="ghost"
  className="md:hidden h-12 w-12 p-0 touch-manipulation"
  style={{ minHeight: '48px', minWidth: '48px' }}
  onClick={() => setIsMobileMenuOpen((open) => !open)}  // <-- key change
  aria-controls="mobile-menu"
  aria-label={isMobileMenuOpen ? "Close mobile menu" : "Open mobile menu"}
  aria-expanded={isMobileMenuOpen}
>
  {isMobileMenuOpen ? <X className="h-7 w-7" /> : <Menu className="h-7 w-7" />}
</Button>


(You don’t need e.preventDefault() or stopPropagation() here.)

2) Ensure the overlay sits above the header and is aligned to it

Replace your overlay block with this animated one (uses the same h-20 → top-20):

{/* Mobile Navigation Dropdown Overlay */}
<div
  id="mobile-menu"
  className={`md:hidden fixed inset-x-0 top-20 w-full bg-white dark:bg-black border-b border-gray-200 dark:border-gray-800 shadow-lg transition-[max-height,opacity] duration-200 overflow-hidden ${isMobileMenuOpen ? "max-h-[85svh] opacity-100 z-[60]" : "max-h-0 opacity-0 pointer-events-none z-[60]"}`}
>
  <div className="px-4 py-6 space-y-4 max-h-[calc(100vh-5rem)] overflow-y-auto">
    {navItems.map((item) => (
      <Link
        key={item.path}
        href={item.path}
        className={`block py-3 px-4 rounded-lg text-lg font-medium transition-colors ${
          location === item.path
            ? "bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400"
            : "text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-800"
        }`}
        onClick={() => {
          setIsMobileMenuOpen(false);
          setLocation(item.path);
        }}
      >
        <span className="mr-3">{item.icon}</span>
        {item.label}
      </Link>
    ))}

    <button
      onClick={() => { setLocation('/donate'); setIsMobileMenuOpen(false); }}
      className="w-full mt-4 py-3 px-4 rounded-lg text-white font-medium"
      style={{ backgroundColor: '#10b981' }}
    >
      Donate
    </button>
  </div>
</div>


Why this works

top-20 matches your h-20 header (no hardcoded 80px guess).

z-[60] puts the menu above the header (z-50).

Transitioned max-height + opacity prevents “nothing happened” feel.

3) Keep auto-close on route change (your effect is good)
useEffect(() => {
  setIsMobileMenuOpen(false);
}, [location]);

4) Quick sanity checks

If you use any backdrop-blur or transform on the header, give the overlay a higher z-index (we did) or remove transforms on the header.

Make sure your page has <meta name="viewport" content="width=device-width, initial-scale=1" /> so md:hidden behaves as expected on phones.

If it’s still not opening, tell me which:

icon changes but menu isn’t visible (likely z-index/offset), or

icon doesn’t change (toggle isn’t firing → wrapper intercepting clicks).