Mobile quick wins (super impactful, tiny changes)
Problem: fixed message height h-96 and the input not sticky → feels cramped / jumps on mobile.

File: client/src/pages/simple-chatroom.tsx – tweak the layout:

Swap the fixed height for viewport-based height:

tsx
Copy
Edit
<CardContent className="p-4">
  <div className="max-h-[60vh] sm:max-h-[70vh] overflow-y-auto space-y-3 mb-4 pr-1">
    {/* messages */}
  </div>
  {/* composer ... */}
</CardContent>
Make the composer sticky at the bottom (so the keyboard doesn’t hide it):

tsx
Copy
Edit
<Card className="mb-4">
  <CardContent className="p-4">
    <div className="max-h-[60vh] sm:max-h-[70vh] overflow-y-auto space-y-3 mb-4 pr-1">
      {/* messages */}
      <div ref={messagesEndRef} />
    </div>

    <div className="sticky bottom-0 bg-background pt-2">
      <form onSubmit={handleSendMessage} className="flex gap-2">
        <Input
          value={messageText}
          onChange={(e) => setMessageText(e.target.value)}
          placeholder="Type your message..."
          inputMode="text"
          className="flex-1"
        />
        <Button type="submit" disabled={!messageText.trim() || sendMessageMutation.isPending}>
          <Send className="w-4 h-4" />
        </Button>
      </form>
    </div>
  </CardContent>
</Card>
Larger tap targets / tighter avatars on small screens:

tsx
Copy
Edit
<CardHeader>
  <div className="flex items-center gap-3 sm:gap-4">
    <Button variant="ghost" size="icon" className="h-9 w-9 sm:h-8 sm:w-8" onClick={() => window.history.back()}>
      <ArrowLeft className="h-5 w-5" />
    </Button>
    <div className="min-w-0">
      <CardTitle className="text-lg sm:text-xl truncate">{chatroom?.name || `Chatroom ${chatroomId}`}</CardTitle>
      {chatroom && <p className="text-xs sm:text-sm text-muted-foreground truncate">
        {chatroom.city}, {chatroom.state}
      </p>}
    </div>
    {/* join/leave buttons ... */}
  </div>
</CardHeader>
For bubbles, allow wrapping and avoid overflow:

tsx
Copy
Edit
<div className="break-words whitespace-pre-wrap leading-relaxed">{message.content}</div>
Sanity checklist (2 minutes)
Reload the page, open DevTools → Network → POST /simple-chatrooms/:id/messages → confirm x-user-id header present.

Confirm server log prints [simple] POST ... uid: X.

Watch your member count increase/decrease after join/leave (the list/count queries will refetch).

Try on your phone—composer should pin to bottom; messages area should scroll.

If anything still acts dead after these changes, send me:

The request/response from the Network tab for the failing POST (headers + status + body), and

The last 20 lines of your server log after you click Send.