Layout/overflow in the About card (button overlapping; nested scrollbars).

Edit Bio UI that isn’t mobile-friendly (goes off-screen).

Below are drop-in fixes. They’re minimal and safe.

A) Fix the About header so nothing overlaps (mobile-first)

Replace your current About header block with this (only the <CardHeader> content):

<CardHeader className="pb-2">
  <div className="flex flex-wrap items-start justify-between gap-3">
    <h3 className="text-lg font-semibold min-w-0 break-words">
      About {user?.userType === "business"
        ? (user?.businessName || user?.name || user?.username)
        : (user?.username || "User")}
    </h3>

    {isOwnProfile && (
      <Button
        size="sm"
        variant="secondary"
        className="shrink-0"
        onClick={() => setIsEditMode(true)}
      >
        <Edit2 className="h-4 w-4 mr-2" />
        Edit Bio
      </Button>
    )}
  </div>
</CardHeader>


Key changes:

flex-wrap + gap prevents overlap.

min-w-0 break-words lets long names wrap instead of pushing the button.

shrink-0 keeps the button from compressing into weird positions.

Also: give your <Card> and <CardContent> no horizontal overflow (this kills those inner scrollbars).

<Card className="mt-6 overflow-visible">
  <CardContent className="overflow-visible">
    …
  </CardContent>
</Card>


Inside the “From: Austin, Texas…” line, ensure the container uses wrapping:

<div className="mt-2 text-sm text-muted-foreground flex flex-wrap items-center gap-x-3 gap-y-1">
  <span className="whitespace-normal break-words">
    From:&nbsp;
    <span className="font-medium">
      {[user?.hometownCity, user?.hometownState, user?.hometownCountry].filter(Boolean).join(", ")}
    </span>
  </span>
  {/* Age, other chips, etc. */}
</div>


If you have little floating “inline edit” chips (the black pill with the ✎ in your screenshot), make them static on mobile so they don’t hover over text:

{/* Example inline edit chip */}
<div className="relative">
  <button
    className="hidden sm:inline-flex absolute -right-2 -top-2 rounded-full px-2 py-1 text-xs bg-black/80 text-white"
    onClick={() => setIsEditMode(true)}
  >
    <Edit2 className="w-3 h-3" />
  </button>
</div>


Note the hidden sm:inline-flex: on phones it disappears and you use the top-right Edit Bio button instead.

B) Make the Edit Bio UI truly mobile-friendly

Use a Sheet on phones (slides up, full width), Dialog on desktop. Here’s a compact component you can drop in and call.

// imports (add if you don't already have them)
import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetFooter } from "@/components/ui/sheet";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from "@/components/ui/dialog";
import { Textarea } from "@/components/ui/textarea";
import { useEffect, useState } from "react";

function useIsSmall() {
  const [isSmall, setIsSmall] = useState(false);
  useEffect(() => {
    const mq = window.matchMedia("(max-width: 639px)"); // < sm
    const update = () => setIsSmall(mq.matches);
    update();
    mq.addEventListener("change", update);
    return () => mq.removeEventListener("change", update);
  }, []);
  return isSmall;
}

export function EditBioModal({
  open,
  onOpenChange,
  initialBio,
  onSave,
  maxLength = 800
}: {
  open: boolean;
  onOpenChange: (v: boolean) => void;
  initialBio?: string;
  onSave: (bio: string) => Promise<void> | void;
  maxLength?: number;
}) {
  const isSmall = useIsSmall();
  const [bio, setBio] = useState(initialBio ?? "");
  const remaining = maxLength - bio.length;

  useEffect(() => {
    if (open) setBio(initialBio ?? "");
  }, [open, initialBio]);

  const Contents = (
    <div className="flex flex-col h-full">
      <div className="flex-1 overflow-y-auto p-4">
        <label className="block text-sm font-medium mb-2">Your bio</label>
        <Textarea
          value={bio}
          onChange={(e) => {
            const v = e.target.value;
            if (v.length <= maxLength) setBio(v);
          }}
          className="min-h-[160px] resize-vertical"
          placeholder="Tell people about yourself, what you enjoy, and how you like to travel…"
        />
        <div className="mt-2 text-xs text-muted-foreground">{remaining} characters left</div>
      </div>

      <div className="border-t p-3 sticky bottom-0 bg-background">
        <div className="flex items-center justify-end gap-2">
          <Button variant="ghost" onClick={() => onOpenChange(false)}>Cancel</Button>
          <Button
            onClick={async () => {
              await onSave(bio.trim());
              onOpenChange(false);
            }}
          >
            Save
          </Button>
        </div>
      </div>
    </div>
  );

  if (isSmall) {
    return (
      <Sheet open={open} onOpenChange={onOpenChange}>
        <SheetContent side="bottom" className="h-[85vh] p-0 overflow-hidden">
          <SheetHeader className="p-4 pb-0">
            <SheetTitle>Edit Bio</SheetTitle>
          </SheetHeader>
          {Contents}
        </SheetContent>
      </Sheet>
    );
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-lg w-[min(92vw,640px)] p-0 overflow-hidden">
        <DialogHeader className="p-4 pb-0">
          <DialogTitle>Edit Bio</DialogTitle>
        </DialogHeader>
        {Contents}
      </DialogContent>
    </Dialog>
  );
}


How to use it in your About card:

// state
const [isEditMode, setIsEditMode] = useState(false);

// button already calls setIsEditMode(true)

<EditBioModal
  open={isEditMode}
  onOpenChange={setIsEditMode}
  initialBio={user?.userType === "business" ? (user?.businessDescription || user?.bio || "") : (user?.bio || "")}
  onSave={async (bio) => {
    await apiRequest("PATCH", `/api/users/${user?.id}`, { bio }); // adjust to your API
    queryClient.invalidateQueries({ queryKey: ["user", user?.id] });
    toast({ title: "Bio updated" });
  }}
/>


Why this works:

On phones it becomes a bottom sheet (h-[85vh], scrollable content, sticky footer).

On desktop it’s a constrained dialog (w-[min(92vw,640px)]).

No more off-screen forms.

C) Kill the double vertical scrollbars

Double bars = nested scroll containers. Ensure only your page scrolls:

In your app shell:

<html className="overflow-x-hidden">
<body className="overflow-x-hidden">
  {/* In your layout */}
  <main className="min-h-screen w-full overflow-x-clip">{children}</main>
</body>
</html>


Avoid overflow-auto or max-h-* on Card, CardContent, header gradients, or wrappers unless you absolutely need local scrolling. Prefer overflow-visible in those places (as applied above).

If you paste the block that renders the “From: Austin… Age: …” row and any “inline edit chip” markup, I’ll tweak those exact lines so they wrap cleanly and stop hovering over your text on small screens.