/**
 * Shared props type for profile page split components.
 * Parent (profile-complete.tsx) passes all state and handlers via a single object.
 */
export type ProfilePageProps = Record<string, unknown>;
